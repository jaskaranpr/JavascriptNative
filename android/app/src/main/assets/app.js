setColor(WHITE);
drawRect(0, 0, SCREEN_WIDTH, SCREEN_HEIGHT);

setColor(BLUE);
drawCircle(250, 250, 100);

setColor(RED);
drawRect(100, 100, 150, 150);

setColor(GREEN);
drawCircle(600, 600, 200);

setColor(RED);
drawText("Jaskaran First Text from skia",300, 200, 50, 0xFF0000FF)
